Make sure current directory is 3-identityref:
$ pwd
> [...]/test/2-mini

Generate java classes using JPyang:
$ pyang -f jpyang -d src/gen confd/identity-ref.yang
> 

Java and schema classes should be generated:
$ ls src/gen
> C.java    Idref.java   My-type.java    package-info.java
  Idref.schema

Set configuration so working directory defaults to src/gen

Create an eclipse project and run the testRun method in test/app/MainTest.java
